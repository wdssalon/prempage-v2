{
  "metadata": {
    "title": "{{ cookiecutter.seo_title }}",
    "description": "{{ cookiecutter.seo_description }}"
  },
  "fonts": [
    {
      "id": "serif",
      "loader": "{{ cookiecutter.serif_font_loader }}",
      "variable": "{{ cookiecutter.serif_font_variable }}",
      "options": {
        "subsets": ["latin"],
        "display": "{{ cookiecutter.serif_font_display }}"
      }
    },
    {
      "id": "body",
      "loader": "{{ cookiecutter.body_font_loader }}",
      "variable": "{{ cookiecutter.body_font_variable }}",
      "options": {
        "subsets": ["latin"],
        "display": "{{ cookiecutter.body_font_display }}"
      }
    }
  ],
  "colors": {
    "bg_base": "{{ cookiecutter.colors.bg_base }}",
    "bg_surface": "{{ cookiecutter.colors.bg_surface }}",
    "bg_contrast": "{{ cookiecutter.colors.bg_contrast }}",
    "text_primary": "{{ cookiecutter.colors.text_primary }}",
    "text_secondary": "{{ cookiecutter.colors.text_secondary }}",
    "text_inverse": "{{ cookiecutter.colors.text_inverse }}",
    "brand_primary": "{{ cookiecutter.colors.brand_primary }}",
    "brand_secondary": "{{ cookiecutter.colors.brand_secondary }}",
    "accent": "{{ cookiecutter.colors.accent }}",
    "border": "{{ cookiecutter.colors.border }}",
    "ring": "{{ cookiecutter.colors.ring }}",
    "critical": "{{ cookiecutter.colors.critical }}",
    "critical_contrast": "{{ cookiecutter.colors.critical_contrast }}"
  }
}
